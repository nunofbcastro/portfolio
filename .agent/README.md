# Agent Configuration

This project uses `.agent` to organize AI behavior into:

- `rules/`: passive constraints always applied
- `workflows/`: user-triggered command sequences
- `skills/`: agent-selected capabilities based on task intent

Use this structure to keep global constraints separate from optional automation.
